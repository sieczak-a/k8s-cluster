# Terraform files
*.tfstate
*.tfstate.*
*.tfvars

# Crash log files
crash.log

# Exclude all .tfvars files, which are likely to contain sensitive data,
# such as password, private keys, and other secrets. These should not be
# in version control.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used for local customization
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Providers file
.terraform*
